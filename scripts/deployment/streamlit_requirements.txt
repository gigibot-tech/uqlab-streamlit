# Deprecated — use uv from the repo root instead:
#   uv sync
#   ./start.sh
#
# Installs uqlab[ui] from the workspace (torch, streamlit, sklearn, …) plus scipy/kaleido.

-e ./src/uqlab[ui]
scipy>=1.10.0
kaleido>=0.2.1
requests>=2.31
